(import-macros "testing"
  (test test-compiles is-equal is-thrown includes))
(import "testing/helpers.js" ((alias compile-kernel compile)))

(test-compiles "regex: pattern only"
  "(regex \"^hello\")" "/^hello/;")

(test-compiles "regex: pattern + flags"
  "(regex \"^hello\" \"gi\")" "/^hello/gi;")

(test "regex: wrong arity throws"
  (is-thrown (compile "(regex)") Error))

(test "regex: non-string pattern throws"
  (is-thrown (compile "(regex foo)") Error))

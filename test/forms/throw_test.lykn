(import-macros "testing"
  (test test-compiles is-equal is-thrown includes))
(import "testing/helpers.js" ((alias compile-kernel compile)))

(test-compiles "throw: basic"
  "(throw (new Error \"oops\"))" "throw new Error(\"oops\");")

(test "throw: wrong arity throws"
  (is-thrown (compile "(throw)") Error))

# DD-25.1 Phase 5: Book Update — Nested Destructuring and Defaults

## Context

DD-25 updated Ch15 §3 to show flat destructured params. DD-25.1 adds nested patterns and defaults — both need book coverage.

---

## Milestone 1: Update section 3 with defaults

**File**: `/Users/oubiwann/lab/cnbb/lykn/src/part3/chapter15/3-parameter-destructuring.md`

### 1.1 — Add "Default Values" subsection

After the "Opting Out with `:any`" subsection, add:

```markdown
### Default Values in Destructured Parameters

Use `(default :type name value)` inside a destructuring pattern to provide fallback values:

\`\`\`lisp
(func create-user
  :args ((object :string name
                 (default :number age 0)
                 (default :string role "viewer")))
  :returns :object
  :body (obj :name name :age age :role role))
\`\`\`

\`\`\`javascript
function createUser({name, age = 0, role = "viewer"}) {
  if (typeof name !== "string")
    throw new TypeError("createUser: arg 'name' expected string, got " + typeof name);
  if (typeof age !== "number" || Number.isNaN(age))
    throw new TypeError("createUser: arg 'age' expected number, got " + typeof age);
  if (typeof role !== "string")
    throw new TypeError("createUser: arg 'role' expected string, got " + typeof role);
  return {name, age, role};
}
\`\`\`

Type checks fire on the final value — after JavaScript applies the default. So if the caller passes `undefined` for `age`, it defaults to `0`, and the `:number` check passes on `0`.

Defaults also work in array destructuring:

\`\`\`lisp
(func pad-triple
  :args ((array :number first
                (default :number second 0)
                (default :number third 0)))
  :body (+ first second third))
\`\`\`
```

---

## Milestone 2: Add nested destructuring subsection

### 2.1 — Add "Nested Destructuring" subsection

After the "Default Values" subsection:

```markdown
### Nested Destructuring

For object destructuring, use `alias` to name the intermediate binding and provide a nested pattern:

\`\`\`lisp
(func process-order
  :args ((object :string id
                 (alias :any customer (object :string name :string email))
                 :number total))
  :returns :string
  :body (template name " (" email ") — $" total))
\`\`\`

\`\`\`javascript
function processOrder({id, customer: {name, email}, total}) {
  if (typeof id !== "string")
    throw new TypeError(...);
  if (typeof name !== "string")
    throw new TypeError(...);
  if (typeof email !== "string")
    throw new TypeError(...);
  if (typeof total !== "number" || Number.isNaN(total))
    throw new TypeError(...);
  return `${name} (${email}) — $${total}`;
}
\`\`\`

The `alias` form names the property key (`customer`) and provides the nested pattern. Type checks target the leaf fields (`name`, `email`), not the intermediate object.

In array destructuring, nesting is positional — no `alias` needed:

\`\`\`lisp
(func process-pair
  :args ((array (object :string name) :number score))
  :body (console:log name score))
\`\`\`

\`\`\`javascript
function processPair([{name}, score]) {
  // ...type checks...
  console.log(name, score);
}
\`\`\`
```

---

## Milestone 3: Update edge cases

**File**: `/Users/oubiwann/lab/cnbb/lykn/src/part3/chapter15/6-edge-cases.md`

### 3.1 — Update the "Destructured `func` Parameters" section

Add:
- Nested patterns in object destructuring require `alias` — bare `(object ...)` is an error
- Defaults and nesting can be combined in the same pattern
- Default values survive `--strip-assertions` (they're runtime semantics, not assertions)
- `(rest (default ...))` is an error — rest collects all remaining elements

---

## Milestone 4: Verify all book examples compile

```bash
# Each example from the book
deno eval '...'  # test each code snippet
```

---

## Files modified

| File | Change |
|------|--------|
| `/Users/oubiwann/lab/cnbb/lykn/src/part3/chapter15/3-parameter-destructuring.md` | Add defaults and nesting subsections |
| `/Users/oubiwann/lab/cnbb/lykn/src/part3/chapter15/6-edge-cases.md` | Add nesting/default edge cases |

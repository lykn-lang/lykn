# CC assignment — Project07 / Arc01 / Slice01

Prepare the versioned baseline and reproducible research protocol for Lykn's
JSON/YAML core-library work. Execute only this slice. **Expedited Mode is on.**

## Start here

Worktree: `/Users/oubiwann/lab/lykn/lang/.worktrees/planning`

Project: `project07-core-library-research`

Read, in order:

1. Root `AGENTS.md` and this project's `project-plan.md` and `ledger.md`.
2. `arc01-format-runtime-evidence/arc-plan.md` and its `ledger.md`.
3. This slice's `slice-plan.md`, `ledger.md`, and
   `artifacts/starting-evidence.md`.
4. Installed collaboration-framework/project-management guides README and
   its Expedited Mode section; work-verification ledger/evidence guides.
5. Scientific-methods inquiry framing, controls, operational measures,
   protocol design, evidence capture, and threats-to-validity guides.
6. Actual Lykn `assets/ai/SKILL.md` and the guide paths selected by the
   baseline inspection. Record exactly which files and revisions you loaded.

The source worktree and CLI versions may differ. Inspect current worktree
registration/status before relying on any previous path or commit. Preserve
unrelated changes. Root source governance forbids authored work on `main`.

## Assignment

Deliver every artifact and ledger criterion in `slice-plan.md`. Reconcile the
prior discussion against current primary sources and pinned package files.
Do not implement libraries or change the language/book/guides in this slice.
Those corrections are explicitly in the overall project and must survive
into their owning work. Do not assume that `@std`, JSR availability, or a
package's marketing summary establishes its dependency or preservation policy.

Select an available, identified research toolchain and document gaps. Locate
the patch checkout if present without inventing its path or creating a repo.
Define concrete JSON/YAML and runtime fixtures, including failure/resource
cases and separate data/byte preservation criteria. Give every case an owner.
Use Lykn for research programs; record and escalate an actual capability gap
through the project before substituting another language or adopting npm.

Define authoring evaluation around retained original tasks, generated Lykn,
actual guide use, book locators, operator reactions, and corrected outcomes.
Separate guide error, ambiguity, omission, agent noncompliance, implementation
defect, and unresolved cause. Specify denominators before reporting frequency.
Controlled comparisons hold other conditions fixed and disclose unavoidable
model/context/toolchain changes. Never infer operator acceptance from silence.

## Exact allowed planning paths

Paths below are relative to the planning worktree. These are the only paths
eligible for this slice's commits, and only if actually changed by this work:

```text
project07-core-library-research/project-plan.md
project07-core-library-research/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/arc-plan.md
project07-core-library-research/arc01-format-runtime-evidence/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/slice-plan.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/cc-prompt.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/starting-evidence.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/baseline.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/source-register.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/research-protocol.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/fixture-matrix.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/authoring-evaluation.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/findings.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/closing-report.md
backlog/discoveries.md
```

Ancestor plan/ledger changes are limited to status, evidence pointers, and
explicitly recorded bubble-ups. Amend plans openly with Version History;
preserve the original scope. `backlog/discoveries.md` is eligible only for
substantiated, deduplicated findings under its protocol. No mass staging.
CDC owns the future `cdc-verification.md`, not this executing context.

## Verification and completion

1. Check every ledger criterion using its stated route. Preserve commands,
   dates, outputs, source locators, and limits in the artifacts.
2. Review all new relative links and source-context citations. Check artifact
   completeness and scope coverage; run whitespace checks on the final diff.
3. When the work is ready, write `closing-report.md` with the per-row walk,
   explicit **Bubble-up to the arc**, unresolved decisions, and exact replay
   instructions. No empty close documents at opening.
4. Keep row status `open` with attested/proposed-done notes until independently
   reproduced; explain any proposed deferral/no-op and its re-entry condition.
5. Inspect staged content. Commit only the exact changed paths above, without
   sweeping other contributors' staged work, **before CDC review**. Include
   both repository co-author trailers in one contiguous block.
6. Report the commit, produced artifacts, checks, remaining concerns, and
   proposed-done status. Do not claim independent verification of your own work.

CDC will reproduce the evidence, commit review/changes, close when justified,
and immediately open the next slice. The next planned assignment is
`arc01-format-runtime-evidence/slice02-json-behavior/cc-prompt.md` relative to
the project; it does not exist yet and must receive a full open set at that
time. No shortcuts, no skipped validation, no weaker evidence or review, no
inferred source scope and no reduction or other change in scope, no timeline
interpretation; operator approval gates are not overridden.

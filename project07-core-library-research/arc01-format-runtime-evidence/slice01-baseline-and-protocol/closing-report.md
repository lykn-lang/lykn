# Slice01 closing report — CC proposed-done

Date: 2026-09-12 UTC. Executing role: CC (Codex). **Not independently verified.**
All seven slice ledger statuses remain `open`; this report attests proposed-done
for the baseline/protocol contract only. CDC owns reproduction, formal closure
and the next full open set. No `cdc-verification.md` was authored.

Planning base: `4b289ba4e8800f99fa4a4f734e122216da76a0ca` on `planning`.
This close packet's commit is the commit containing this report; resolve with
`git log -1 --format=%H -- project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/closing-report.md`
from planning. The final operator report supplies its resulting hash.
No source commit, dependency adoption or publication was made.

## Artifact inventory

Six new durable artifacts, all under the owning slice:

- [baseline.md](artifacts/baseline.md): local/toolchain/guide/book identity,
  selected binary hashes, commands and limits.
- [source-register.md](artifacts/source-register.md): SE-01–10 dispositions,
  primary URLs, pins/hashes, static import boundaries and replay.
- [research-protocol.md](artifacts/research-protocol.md): RP01 setup, isolated
  execution, launch/candidate gates, evidence records, controls and stop rules.
- [fixture-matrix.md](artifacts/fixture-matrix.md): FM01, 51 owned case families
  (17 JSON, 16 YAML, 9 runtime, 9 Lykn/authoring).
- [authoring-evaluation.md](artifacts/authoring-evaluation.md): AE01, five exact
  task texts, actual-reference logs, taxonomy, denominators and human review.
- [findings.md](artifacts/findings.md): five confirmed discoveries, hypotheses,
  deduplication evidence and unresolved prerequisites.

The original [starting evidence](artifacts/starting-evidence.md) and CC prompt
remain unchanged. Downloads and draft text in `/private/tmp` are disposable;
all durable claims have committed artifacts, pinned source locators and replay
commands. No extra research programs or fixture files were created.

## Per-row verification walk

| Row | CC disposition / evidence | Limit and independent route |
| --- | --- | --- |
| S-01 | Proposed-done, attested. B01 records actual source paths/commits/status, installed and local CLI, Deno/host, book symlink and guide blobs | CDC reruns identity commands. Patch not located in bounded search; source-to-binary build provenance unknown, explicitly permitted gaps |
| S-02 | Proposed-done, attested. Source register reconciles all 10 seed IDs with pinned source or specification; qualified framing and live Node API findings retained | CDC inspects links/functions, manifest hashes and evidence kind. No API inspection is reported as a Lykn execution |
| S-03 | Proposed-done, attested. FM01 covers project scope and maps every family to an owner, input, invariant/unknown and evidence | CDC walks scope crosswalk. Case-family expansion is predeclared future work; blocked candidate/resource variants remain owned |
| S-04 | Proposed-done, attested. RP01 identifies existing B01 compiler/runtime and isolated paths, commands, permissions, captures, resource controls and stops | Read-only walkthrough confirms available identity/help paths. Runtime command is deliberately gated by the actual PERM source defect; future opening must resolve it, not pretend a working least-privilege CLI route exists |
| S-05 | Proposed-done, attested. AE01 retains five original task texts, book locators, generated-code/reference logging, six-cause taxonomy, denominators and controlled corrections | CDC checks executability of evaluation design subject to shared launch gate. Zero operator reviews/acceptances or causal results claimed |
| S-06 | Proposed-done, attested. Five deduplicated open permanent IDs; hypotheses/prerequisites separately owned. All new discoveries remain open rather than claiming future Git routing | CDC checks register diff and primary locations. No repair/acceptance inferred; existing try/parity/book discoveries not duplicated |
| S-07 | Proposed-done, attested. Six artifacts and this close packet; ancestor status/evidence/bubble-up changes; exact-path commit and final validation record below | CDC inspects landed diff and repeats checks. All seven rows retained; no deferred/no-op Slice01 rows; later work remains open |

## Verification record and replay

Inspection on 2026-09-12 produced:

- Worktree/status/identity checks: clean source/release/planning before edits;
  book `_to_delete/` untracked and preserved. B01 has full commits/hashes and
  output values. No source worktree rebuild/upgrade performed.
- Package retrieval: initial browser/sandbox failures recorded; authorized
  public curl retrieval succeeded. @eemeli config guessed URLs returned 404,
  corrected via actual manifest to jsr.jsonc. No downloaded code executed.
- Manifest integrity: 30/30 @std/yaml TS files and 77/77 @eemeli/yaml TS files
  matched pinned metadata SHA-256; 0 mismatches. Live Node process imports are
  source evidence, not runtime validation.
- Coverage self-review: 10 distinct SE rows; 51 distinct fixture families;
  seven original ledger criteria addressed; five task texts; five new open IDs.
- Final relative-link, source-locator, whitespace and exact staged-scope checks
  are recorded below after their final pass.

Replay B01's local commands; then source-register's pinned curl/hash/source
walk. Read RP01 from setup through stop rules without launching a format probe;
confirm help/paths and verify `git show 8c66469:crates/lykn-cli/src/main.rs`
against the permission gate. Cross-check FM01 against project Scope and AE01
against P-02–P-05. Check every new local link; source citations use their own
repository/commit. No source `make check` or format corpus was run: these are
planning/research artifacts, and source changes/full format execution are
outside this slice. This does not count as a source validation pass.

## Exact commit scope

Paths relative to planning; all actually changed and all explicitly allowed by
the assignment:

```text
project07-core-library-research/project-plan.md
project07-core-library-research/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/arc-plan.md
project07-core-library-research/arc01-format-runtime-evidence/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/slice-plan.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/ledger.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/baseline.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/source-register.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/research-protocol.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/fixture-matrix.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/authoring-evaluation.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/findings.md
project07-core-library-research/arc01-format-runtime-evidence/slice01-baseline-and-protocol/closing-report.md
backlog/discoveries.md
```

Inspect cached names, cached diff and `git diff --cached --check` before commit.
No blanket staging; do not include unrelated staged content. Include both
repository co-author trailers in one contiguous block. No push authorized.

## What worked

Pinning manifests and inspecting imported implementation files exposed a Node
API edge hidden by candidate registry/API summaries. Separating source commit
from ignored binary identity avoided overstating build provenance. Keeping the
original seed preserved which claims were confirmed and which needed narrower
interpretation. The coverage crosswalk retained authoring and human outcomes
alongside format research rather than postponing their design.

## Bubble-up to the arc

1. **Assigned capability delivered as proposed-done:** Slice01 supplied baseline,
   source reconciliation and reproducible protocols. No format behavior or
   library capability is claimed from this baseline slice alone.
2. **New implications:** D-2609-PERM requires a launcher correction or explicitly
   authorized execution route before S02 runtime work; CDC may open Arc03 early
   under existing project authority. D-2609-YNOD prevents execution/adoption of
   the pinned YAML document candidate under current policy; retain Y-14 and
   resolve alternatives in S04. D-2609-LINT/NPMB/JSER carry concrete guide/book
   corrections into A02/A03. Patch identity and binary build provenance remain
   explicit. Ancestor plans were updated with v1.1 entries and evidence pointers.
3. **Silent-drop diff:** all six slice scope items and seven ledger rows have
   artifacts. No Slice01 deferral/no-op requested. Later JSON/YAML execution,
   runtime graph closure, language/ADT trials, accepted source corrections and
   operator acceptance remain in their original owning work. Resource thresholds
   bound trials, not project scope; larger/unsupported cases need dispositions,
   not erasure. No unrelated source edit or new project ID introduced.

After independent reproduction, CDC closes only if warranted and opens the
next full slice with explicit handling of these prerequisites. The roadmap's
next prompt is still:

`arc01-format-runtime-evidence/slice02-json-behavior/cc-prompt.md`

It does not exist yet; CC did not open it or any CDC verification artifact.

## Final self-check results

2026-09-12: **49 local links checked, 0 missing** across slice Markdown and
updated project/arc plans and ledgers (fenced code excluded); **17 pinned
source/book locators checked, 0 missing** using `git cat-file -e`. The six new
register-to-artifact links resolve too. Ten distinct SE rows, 51 distinct case
families (no duplicate IDs), seven slice ledger rows, five AT tasks and five new
register headings were checked. Source worktrees remain clean; the book still
has only its pre-existing `_to_delete/` untracked state.

The staged path list matched the 14-path scope exactly and was a subset of the
prompt's allowlist. Cached diff review covered artifacts, ledger/status changes
and the append-only discovery addition. `git diff --check` and
`git diff --cached --check` passed with no output. The first local-link pass ran
before this closing report existed and flagged its pending links; the final
pass above includes the complete packet and ignores regex examples in fences.

Repeat the local-link check from the project directory (Bash):

```sh
link_errors=0
link_checked=0
while IFS= read -r item; do
  source_file=${item%%:*}
  link=${item#*:}
  link=${link#']('}
  link=${link%')'}
  case "$link" in http:*|https:*|mailto:*|'#'*) continue ;; esac
  target=${link%%#*}
  link_checked=$((link_checked+1))
  if [ ! -e "$(dirname "$source_file")/$target" ]; then
    printf 'MISSING %s -> %s\n' "$source_file" "$target"
    link_errors=$((link_errors+1))
  fi
done < <(awk 'FNR==1 {fence=0} /^```/ {fence=!fence; next} !fence {line=$0; while(match(line,/\]\([^)]*\)/)) {print FILENAME ":" substr(line,RSTART,RLENGTH); line=substr(line,RSTART+RLENGTH)}}' project-plan.md ledger.md arc01-format-runtime-evidence/arc-plan.md arc01-format-runtime-evidence/ledger.md arc01-format-runtime-evidence/slice01-baseline-and-protocol/*.md arc01-format-runtime-evidence/slice01-baseline-and-protocol/artifacts/*.md)
printf 'relative_links=%s missing=%s\n' "$link_checked" "$link_errors"
test "$link_errors" -eq 0
```

Source locator replay: for every B01/AE01 source path use
`git -C /Users/oubiwann/lab/lykn/lang cat-file -e 8c66469:PATH`; for the seven
book paths use `git -C /Users/oubiwann/lab/lykn/book cat-file -e 6aa379d:PATH`.
Inspect the stated sections/lines after existence checks; existence alone is
not claim verification. After commit, use `git show --check COMMIT` and compare
`git diff-tree --no-commit-id --name-only -r COMMIT` with Exact commit scope.

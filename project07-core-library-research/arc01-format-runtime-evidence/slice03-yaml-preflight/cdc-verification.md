# YAML preflight — independent CDC verification

**CDC closed, 2026-09-12. Six of six criteria reproduced.** Reviewed CC commit
`c6eae6b`; its closing report remains the historical implementer attestation.
This closes preparation only. Full YAML execution and Arc01 composition remain
open; no package adoption, source repair or operator acceptance is inferred.

## Reproduced condition and results

Exported immutable source `d0bb981dae2a4abf6c984406c4b2081a45cc92f8` into
`/private/tmp/lykn-p07-yaml-cdc.HsBg29/source` and built with
`cargo build --release --locked`. The compiler SHA-256 is
`78aac90c4b4d6c644ac376e99ce91f88cdae32477aff476980912e68eba0c1bb`, matching CC.
Deno 2.7.7 matches SHA-256
`103ea70463213b1a8ea7b46852a34612f4e0afcc1ff09d1656d185ed215772d4`.
The live release/0.6.x checkout was clean at `50608c4`; it was neither reset
nor rebuilt. Planning started clean at the reviewed commit.

Fresh trial: `/private/tmp/lykn-p07-yaml-cdc.HsBg29/trial`. The three retained
programs passed check/lint/compile. Reviewed emitted source, including alias
identity, signed-zero handling, guarded traversal, dispatch and file operations.
Known unused-binding warnings remain observable; generated output was not edited.
Both `inputs.json` and `case-manifest.md` regenerated byte for byte through
Lykn. All five declared source/manifest/lock hashes match the CC receipt.

Initial sandbox dependency resolution produced a graph error and host process
observation was denied. Both were retried successfully through the approved
host route before any supervisor control. This was an environment restriction,
not a package or language failure. The fresh graph has 26 modules, the same
25 remote identities, one pinned `@std/yaml@1.2.0` and no Node/npm entries or
resolution errors. The real frozen lock is unchanged. No @eemeli code executed.

Real denied-read control exited 1 with NotCapable; scoped read printed CONTROL.
The separate fake recorder confirmed permission flags before the script and
the trailing `--allow-all` as script data. Real runs used the cleared environment
and original PATH. Wall, output and sampled-RSS controls each exited 137 with
the expected stop reason, positive process observation and no observed survivor.
The output control retained at most 4096 combined bytes.

All **38 sentinel/harness attempts** completed without unexpected exit or cap.
The retained audit reproduced **1,991 checks**. An additional independently
authored [CDC audit](artifacts/cdc-audit.lykn) passed **907 checks**: all 41
CC/CDC records correspond uniquely, all 38 sentinel stdout records match exactly,
and all recorded pre/post file states agree. It also checks every frozen family
count/owner, graph identities, locks, permission prefixes, and actual destination
bytes against the serializer output. Four nonzero harness exits are the expected
unknown-operation/unknown-ID controls. Their diagnostic text is checked without
requiring environment-dependent stack paths to match. Timing, sampled RSS,
process IDs, compiler warning order and stderr timing receipts are not asserted
byte-identical across runs.

The original CC commit contains 20 paths within its 21-path allowlist, all 12
promised artifacts, and no source/book changes. The retained planning audit
reproduced 20 exact-scope checks and 66 local links. Opening and closing ledger
counts are both six; their criteria are unchanged. No deferred or no-op row.
The CDC close/next-open packet is limited to 16 explicit planning paths. Its
63 changed-Markdown local links resolve, all six original preflight criteria
remain verbatim, and the next slice opens seven criteria. Whitespace checks
pass; original CC artifacts and source/book trees are untouched.

## Row walk

| Row | CDC disposition | Reproduced evidence and boundary |
| --- | --- | --- |
| YP-01 | done | Fresh immutable build, byte identity, real frozen package graph and successful package execution; broader provenance/offline policy stays Slice04 |
| YP-02 | done | Exact freeze plus independent count/owner audit; FM01 crosswalk retains 349 YAML variants across all 16 families, including 54 resource and 28 gated document variants |
| YP-03 | done | Source/emitted review and direct CC/CDC output comparison reproduce graph identity, cycles, scalar kinds, positioned errors, tagged inspector controls and byte differences |
| YP-04 | done | Actual permission denial/grant and argv separation; three supervised stop controls precede sentinels; sampled limits remain thresholds, not hard memory or real-time guarantees |
| YP-05 | done | Unmodified retained sources replayed all 38 attempts; 1991 retained-audit checks and 907 additional CDC comparison checks passed through Lykn |
| YP-06 | done | Original/revised authoring evidence and findings retained; ancestor plans updated and Slice05 opened with all full-value obligations; later resource/document/policy and human owners preserved |

## Evidence inventory and replay

The original 12 artifacts remain unchanged. CDC adds the independent audit,
[41 raw replay records](artifacts/cdc-replay.jsonl),
[fresh dependency graph](artifacts/cdc-dependency-graph.json), and
[build/gates/controls/audit receipt](artifacts/cdc-transcript.txt).
The receipt includes the initial denied graph and the successful host retry.

Use the exact [baseline replay](artifacts/baseline-and-protocol.md) with a fresh
source export/trial. Compare both regenerated manifest files. Stop on graph
errors even if cache/info exits zero; check host observation before controls.
Use the authorized host route if the environment restricts observation or
dependency transport. Run controls once, then sentinels once in that trial.
After the retained audit, copy `cdc-audit.lykn` into the trial, run its source
gates, and execute through the pinned CLI:

```sh
yaml_env "$yaml_cli" run --no-prompt --allow-read="$yaml_trial,$artifact_dir" cdc-audit.lykn "$artifact_dir"
```

Here `artifact_dir` is this committed Slice03 artifacts directory. Expected
result: 907 checks, 41 records, 38 stdout comparisons, 349 YAML variants and
259 next-value variants. Scratch paths in records identify the original run;
fresh replay creates its own directories. Raw CDC records and the receipt are
durable even after the original scratch is removed.

CDC used project-management/work-verification closure guides and the pinned
Lykn skill/anti-pattern guide. The small comparison program needed no language
fallback. Manual review corrected surplus closing parentheses before its first
source gate; all subsequent gates and execution passed on their first attempt.
These observations are review provenance, not a controlled guide-error rate.

## Bubble-up to the arc

Preparation delivered its assigned capability. The silent-drop walk preserves
every FM01 Y family: Slice05 owns 259 variants (Y-01–12/Y-15), Slice06 owns 62
(Y-13/Y-16), and Slice07 owns 28 (Y-14), totaling 349. None inherits full-family
closure from sentinels. The six preflight criteria required no scope amendment.

The handoff needs explicit operational detail before full execution:

- Y-01 currently parses literal fixture bytes before verifying the file. Slice05
  must read and parse the materialized input first for the complete workflow.
- Y-09 preparation has no existing destination. Slice05 must exercise the actual
  replacement path with an existing destination and prove rejected parses leave
  it untouched. An accepted duplicate anchor must remain an observed outcome.
- Y-05/Y-06 need serialization, reparse/identity checks, and original/no-op/edit
  byte evidence where the operation is meaningful. Identity probes alone do not
  establish serialization behavior.
- Future handlers intentionally do not exist yet. Slice05 must freeze explicit
  runtime-value construction and operation details before executing its matrix;
  retain YP-M01 IDs, bytes/options and questions with a documented crosswalk.
- Inspector markers describe bounded observations. Map/Set enumeration and
  Object.keys happen before slicing, and emit checks encoded size after building
  it. These are suitable small-case guards, not universal resource guarantees;
  Slice06 and Slice04 must account for inspector/observer overhead separately.

Arc01's body and Version History carry these clarifications before the Slice05
opening. Execution remains 03 → 05 → 06 → 04 → 07. Stable std/yaml has no separate
toJS or Document API; absence cannot be turned into a simulated passing stage.
YNOD still gates the document route. NZRO/JSCF/UNUS and LINT/NPMB/JSER remain
open findings; no new compiler defect is inferred from preflight author errors.
Arc02 retains ADT, guide/book and operator code-shape review. All project rows
and Arc01 A-03–06 remain open.

## What worked

The small preparation slice made baseline, API and authoring mistakes visible
before the larger run. Reading the compiler path from a verified replay parameter
let CDC execute the exact retained source bytes. Lykn generated the full manifest
and audited the evidence without the JSON slice's aggregation-language deviation.

# CC Prompt — arc13 / slice11 · conformance-corpus + dispositions

**From:** CDC (Cowork) · **To:** CC (Claude Code, host) · **Date:** 2026-07-07
**Branch:** `release/0.6.x` (from `c19a1fb` or later). **Re:** Arc13's
**last slice** — make the convergence permanent, and leave zero
ambiguity in the residual. Read first: the slice-doc (the corpus design
question + the five dispositions), slice10's closing report (the
56-cell A-4 baseline), DD-60's ‡ footnote + edge case 4.

## The work (MUST) — 7 rows (ledger has the table)

1. **F-1 — the corpus.** Standing in `make check`: ≥1 cross-backend
   test per name-class × position equivalence class (publish the
   coverage mapping — every class → its test). Vehicle is your call
   **on timing evidence** (`.lykn` compile-both rows per the A-8
   precedent, and/or a matrix-subset snapshot); report the `make check`
   cost delta. The full matrix stays the on-demand audit tool.
2. **F-2 — teeth.** Seeded-divergence demo: temporarily break one
   backend's resolution, watch the corpus fail, restore. Transcript.
3. **F-3 — the `macro` row.** Bound legal-ident `macro` →
   `calls-binding` on Rust too (D1 has no macro-name exception; JS is
   already there). Expected small; **if contact says otherwise, surface
   and stop — don't grow the slice.**
4. **F-4 — label asymmetry + `kernel:if`.** Probe the label-adjacent
   reference asymmetry: D1 violation in disguise, or a documented
   corner? Fix-or-document **with rationale** (document → CDC records
   the DD-60 note; don't edit the DD yourself). `kernel:if` strict
   cells: document (unbindable, DD-60 edge 4).
5. **F-5 — `contains_await`.** Probe first (bound `await` param,
   non-async body: what emits, what should). Then the one-line
   `as_form_head()` change + test, **or** a documented exemption
   rationale. Either is a valid close; deciding without the probe
   isn't.
6. **F-6 — D2-timing residual.** Document the constructed-name
   asymmetry (+ cheap pinning probe if available). Do **not** re-plumb
   pass placement.
7. **F-7 — final snapshot.** `make check` ✓; publish the end-state
   matrix: final divergence count with every residual
   documented-as-intended. **This is the A-4 evidence the arc close
   consumes — write it to be consumed.**

## Discipline

`./bin/lykn build` before every probe (trap #4: the `lang/` import map
resolves to `target/lykn/build/lang/`). Every disposition = probe
evidence → decision → where recorded. Surface anything the corpus
build reveals — this arc's DDs took five refinements from exactly that
discipline. Fresh-context handoff remains a proven move. Closing report
untracked; commit **source only**, green increments. Bubble-up: the
arc-close handoff — final numbers, capability confirmation (or what's
missing), and anything the last slice taught.
